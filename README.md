# Calculator_Authenticator-for-yield-calculation
This simple code calculates the simple yield math on the farm. But before we get there the farmer has to be authenticated. One problem i am yet to fix is "The farmer can still access the calculator even after the authentication fail"
